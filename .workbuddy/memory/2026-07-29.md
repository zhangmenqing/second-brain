# 2026-07-29 工作日志

## 16:13 修复 sync.bat 的 git PATH 问题
- 现象：每小时同步自动化调用 `E:\workbench\sync.bat` 时，bat 内 `git` 命令因子进程 PATH 不含 git 而全部失败（exit 9009），实际从未执行 git 操作。
- 根因：git 是 WorkBuddy 自带 PortableGit（`C:\Users\19712\.workbuddy\vendor\PortableGit\mingw64\bin\git.exe`），不在自动化/cmd 子进程 PATH。
- 修复：在 `sync.bat` 的 `cd /d E:\workbench` 之后加入自动定位 git 并前置 PATH 的逻辑（PortableGit → C:\Program Files\Git\cmd → \Git\bin → x86\Git\bin）。其余 git 调用不变。
- 验证：PowerShell 跑 `sync.bat` 已能正常 commit/pull（日志 `.sync/sync.log` 恢复正常）。
- 残留阻塞：当前 `git push` 失败 exit 128 —— 本地代理 127.0.0.1:7890 离线，github.com 直连不可达。修复提交 `10026b7` 暂未推送（本地 ahead 1），待代理恢复后由下次 hourly 同步自动推送。
- 结论/经验：WorkBuddy 作业环境 bat/cmd 子进程 PATH 不含 git，脚本里动态探测 git 绝对路径更稳妥。

## 16:16 每小时同步自动化（绕过离线代理完成推送）
- 关键更正：本地代理 127.0.0.1:7890 离线，但 `env -u HTTP_PROXY -u HTTPS_PROXY` 后直连 github.com 返回 200——之前 `http_code 000` 是 curl 继承失效代理 env 的假阴性。直连可用，仅代理失效。
- 执行：在 Git Bash 绕过代理跑 sync 等效逻辑，`git add -A && commit && pull --rebase --autostash && push`（git 用 `-c http.proxy= -c https.proxy=`，env 用 `env -u HTTP_PROXY -u HTTPS_PROXY`）。
- 结果：推送成功（0c87fc6..68807dc main->main），本地与 origin/main 对齐（ahead/behind=0/0），工作树干净。
- 经验：本环境 git 配置+环境变量均指向离线代理 7890，常规 git push 必失败；只要代理没起，同步需显式绕过代理或先启动代理。

## 17:15 每小时同步自动化（GitHub 整体不可达）
- 现状：`sync.bat` 存在，但自动化子进程里无法直接跑——Git Bash 下 `cmd //c` 不被 cmd 识别为 `/c`（bat 体未执行），`cmd /c` 又被沙箱安全策略拦截。
- 退路：在 Git Bash 直接跑 bat 等效逻辑并显式绕过代理（`env -u HTTP_PROXY -u HTTPS_PROXY` + `-c http.proxy= -c https.proxy=`，二者缺一不可，否则回退到离线代理）。
- 关键发现：本次 GitHub **整体不可达**——代理 7890 离线，DNS 可解析(20.205.243.166)但直连 443 超时（~21s）。16:16 时直连曾返回 200 应为偶发，当前稳定不可达。
- 结论：仓库自 16:16 推送后已对齐（0 ahead/0 behind，工作树干净），无待同步内容；本次仅无法与远端实时确认。环境恢复后下次同步即可正常 pull/push。
