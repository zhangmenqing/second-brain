# 第二大脑工作台 · 使用手册

> 这是 dean 的个人知识与文件中枢，所有内容都在 `E:\workbench`，通过 GitHub 在两台电脑之间同步。
> 你是主人，我是 Sam（你的第二大脑）。这份手册帮你从零上手。

---

## 一、它解决什么痛点

| 痛点 | 解法 |
|---|---|
| 想法随手记了就忘 | 统一收进 `Inbox`，再定期整理 |
| 两台电脑文件不同步 | Git 自动同步（不是网盘，需主动拉取） |
| 换设备后 AI 没上下文 | `AGENT-BRIEF.md` 负责跨设备交接 |
| 不知从哪开始 | 有固定结构（PARA），照着放就行 |

---

## 二、目录结构（PARA 方法）

```
E:\workbench\
├── Inbox/        # 收件箱：所有新想法、链接、草稿先进这里
├── Projects/     # 有目标+截止期的项目（做完就移走）
├── Areas/        # 长期维护的领域（健康、财务、学习…）
├── Resources/    # 参考资料、模板、素材（随时可查）
├── Archives/     # 已完成 / 归档的内容
├── sync.bat      # 一键同步脚本（双击即用）
├── first-run.bat     # 本机首次注册自动同步
├── setup-other-pc.bat# 新电脑一键部署
└── AGENT-BRIEF.md   # 给 AI（Sam）看的交接说明
```

**铁律：** 新东西一律先丢 `Inbox`，别直接堆在根目录；整理时再往其他区搬。

---

## 三、日常怎么用

1. **记东西**：在 `Inbox` 新建一个 `.md` 或 `.docx`，写标题 + 内容即可。
2. **整理**：每周挑一次时间，把 `Inbox` 里成形的内容挪到 `Projects` / `Areas` / `Resources`，完成的项目挪到 `Archives`。
3. **保存并同步**：双击 `sync.bat`（见下），自动把改动推到云端、并拉取对方设备的更新。

---

## 四、跨设备同步（最关键）

Git 不是网盘：每台电脑各有一份本地副本，**必须 pull 才会拿到另一台的更新**，不会自动出现。

### 方式 A：最简单（推荐日常）
双击 `E:\workbench\sync.bat`
- 内部流程：先 `git pull --rebase` 拉取远程更新 → 再 `git push` 推送本地改动
- 日志写入 `E:\workbench\.sync\sync.log`，出问题可查

### 方式 B：命令行
```cmd
cd E:\workbench
git pull --rebase   # 拉取另一台设备的更新
git push            # 推送本机的改动
```

### 方式 C：全自动（一次注册，长期省心）
- 本机首次：双击 `first-run.bat`
- 换电脑：双击 `setup-other-pc.bat`（会自动 clone 仓库、配代理、注册每小时定时任务）

注册后，每小时自动跑一次同步，基本不用手动。

### 为什么"在另一台看不见"？
另一台 `push` 之后，这台必须 `pull`。如果一直没同步，通常是这台没跑过 `sync`，或定时任务没注册 → **双击 `sync.bat` 即可解决**。

---

## 五、几条铁律（避坑）

- **先拉后改**：在另一台可能改过同一文件的前提下，先 `pull` 再动手，避免冲突。
- **别把大文件塞仓库**：视频、超大压缩包走网盘，仓库只放文本/文档。
- **token 安全**：GitHub 凭据存在 Windows 凭据管理器（wincred），不要明文写进任何文件或截图。
- **冲突不要慌**：若两边同时改了同一文件，pull 会提示冲突，手动合并后重新 `sync` 即可。

---

## 六、给 Sam（AI）的交接

每次换设备、或新开对话，先让 Sam 读 `AGENT-BRIEF.md`，里面写了你的身份、偏好和工作台规则，保证我「换个壳也认得你」。

---

## 七、命令速查

| 想做的事 | 怎么做 |
|---|---|
| 看本地改了什么 | `git status` |
| 看两台是否一致 | `git fetch` 后 `git log HEAD..origin/main` |
| 拉取对方更新 | `git pull --rebase` |
| 推送本机改动 | `git push` |
| 一键同步 | 双击 `sync.bat` |
| 注册自动同步 | 双击 `first-run.bat` / `setup-other-pc.bat` |

---

_最后更新：2026-08-28，由 Sam 整理。_
